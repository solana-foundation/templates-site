pre-commit:
  parallel: true
  jobs:
    - name: Format files
      run: pnpm prettier --write {staged_files}
      exclude:
        - '*.png'
        - '*.svg'
        - 'CODEOWNERS'
        - '.prettierignore'
      stage_fixed: true
